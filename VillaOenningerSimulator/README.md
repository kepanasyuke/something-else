# 🏛️ Симулятор Демонтажа Виллы Альфреда Оеннинга

Интерактивный 3D-проект с ИИ-роботом-сносчиком, следующий Трём законам робототехники.

## 🚀 Быстрый старт в GitHub Codespaces

1. Создайте Codespace для этого репозитория.
2. Установите Unity с модулем WebGL в Codespace или подготовьте готовую сборку WebGL.
3. В терминале выполните:
   ```bash
   cd VillaOenningerSimulator
   make setup
   make UNITY_PATH=/path/to/Unity play
   ```
   Если Unity находится в `PATH`, параметр `UNITY_PATH` можно не указывать.
   В Codespace с Docker-образом Unity используйте:
   ```bash
   make play-docker
   ```
   Для уже готовой сборки достаточно выполнить:
   ```bash
   make serve
   ```
Нажмите на всплывающее уведомление о порте 8000 – игра откроется в браузере.

⚠️ Важно
Проект уже содержит автоматически созданную сцену с базовыми объектами (камера, свет, робот, стены).

Если вы хотите изменить сцену, откройте проект в Unity Editor (локально или через Codespace с графикой).

Команда make build (старая) удалена – используйте только make play.

📂 Структура
Assets/Scripts/ – весь C# код.

Assets/Scenes/SampleScene.unity – автоматически сгенерированная сцена.

Assets/Editor/SceneBuilder.cs – скрипт, создающий сцену при первом запуске.

Создано для демонстрации в облаке.
